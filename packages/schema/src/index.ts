// Generated types land in ./generated/api.d.ts via `pnpm --filter @coolblock/schema generate`
// once apps/api exposes a real OpenAPI schema (Phase 7). Do not hand-write API types here --
// packages/schema is the single source of truth between the FRONT and PLATFORM lanes (§11).
export {};

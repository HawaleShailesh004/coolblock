{
  "name": "coolblock",
  "private": true,
  "description": "Where should the next 40 trees go? A block-scale heat-mitigation siting optimizer.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint && eslint e2e playwright.config.ts --max-warnings 0",
    "typecheck": "turbo run typecheck && tsc --noEmit",
    "test": "turbo run test",
    "e2e": "playwright test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.1",
    "eslint": "^9.15.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^15.12.0",
    "playwright": "^1.63.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.15.0"
  }
}
